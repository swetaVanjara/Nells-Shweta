<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="NellsPay.Send.Views.MoneyTransferFlowPages.PaymentMethodPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:Icons="clr-namespace:NellsPay.Send.Models"
    xmlns:model="clr-namespace:NellsPay.Send.Models.MoneyTransferFlowModels"
    xmlns:viewmodels="clr-namespace:NellsPay.Send.ViewModels.MoneyTransferFlowViewModels"
    x:DataType="viewmodels:PaymentMethodVM"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Background="{AppThemeBinding Light={DynamicResource Neutral100Light}, Dark={DynamicResource AccentBlack}}"
    NavigationPage.HasNavigationBar="False">
    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior
            StatusBarColor="{DynamicResource PageBackground}"
            StatusBarStyle="LightContent"/>
    </ContentPage.Behaviors>
    <Grid
        Padding="16,16,16,0"
        RowDefinitions="Auto,*,Auto"
        RowSpacing="15">
        <Grid ColumnDefinitions="50,*,50">
            <Button
                Grid.Column="0"
                Padding="2,2,2,2"
                BackgroundColor="{AppThemeBinding Light={DynamicResource AccentWhiteLight},
                                                  Dark={DynamicResource AccentWhiteDark}}"
                Command="{Binding BackCommand}"
                Style="{DynamicResource LargeCircularIconButtonStyle}"
                Text="{DynamicResource IconChevronleft}"
                TextColor="{AppThemeBinding Light={DynamicResource Neutral800Light},
                                            Dark={DynamicResource Neutral800Dark}}"/>

            <Label
                Grid.Column="1"
                HorizontalTextAlignment="Center"
                Style="{DynamicResource BodyBold20}"
                Text="Payment method"
                TextColor="{AppThemeBinding Light={DynamicResource Text1Light},
                                            Dark={DynamicResource Text1Dark}}"
                VerticalTextAlignment="Center"/>
        </Grid>
        <VerticalStackLayout Grid.Row="2">
            <Border BackgroundColor="#330066FF" Margin="0,0,0,15"
                    Style="{DynamicResource MainBorderStyle}">
                <Grid ColumnDefinitions="Auto,*"
                        ColumnSpacing="10">
                    <Label
                        FontSize="18"
                        Style="{DynamicResource LabelIconeStyle}"
                        Text="{DynamicResource IconInfo}"
                        TextColor="{AppThemeBinding Light={DynamicResource Information400Light},
                                                    Dark={DynamicResource Information400Dark}}"/>
                    <Label
                        Grid.Column="1"
                        Style="{DynamicResource BodyMedium12}"
                        Text="Please prepare your KYC documents in case we require verification to complete this transfer"
                        TextColor="{AppThemeBinding Light={DynamicResource Information400Light},
                                                    Dark={DynamicResource Information400Dark}}"
                        VerticalTextAlignment="Center"/>

                </Grid>
            </Border>
        </VerticalStackLayout>

        <Grid Grid.Row="1">
            <CollectionView
                x:Name="PaymentListCollection"
                HorizontalScrollBarVisibility="Never"
                ItemsSource="{Binding PaymentList}"
                VerticalScrollBarVisibility="Never">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout ItemSpacing="16"
                            Orientation="Vertical"/>
                </CollectionView.ItemsLayout>
                <CollectionView.Header>
                    <BoxView BackgroundColor="Transparent"
                            HeightRequest="0"/>
                </CollectionView.Header>
                <CollectionView.Footer>
                    <BoxView BackgroundColor="Transparent"
                            HeightRequest="0"/>
                </CollectionView.Footer>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="model:PaymentMethod">
                        <Border
                            Padding="16"
                            StrokeThickness="0"
                            Style="{DynamicResource MainBorderStyle}">
                            <Border.Triggers>
                                <DataTrigger TargetType="Border"
                                        Binding="{Binding Selected}"
                                        Value="True">
                                    <Setter Property="BackgroundColor"
                                            Value="{AppThemeBinding Light={DynamicResource AccentWhiteLight}, Dark={DynamicResource Foundation3Dark}}"/>
                                </DataTrigger>
                                <DataTrigger TargetType="Border"
                                        Binding="{Binding Selected}"
                                        Value="False">
                                    <Setter Property="BackgroundColor"
                                            Value="{AppThemeBinding Light=LightGray, Dark=gray}"/>
                                </DataTrigger>
                            </Border.Triggers>
                            <Grid ColumnDefinitions="50,*"
                                    ColumnSpacing="10">
                                <Border
                                    Margin="0,7,0,10"
                                    Padding="3"
                                     BackgroundColor="{AppThemeBinding Light={DynamicResource Neutral200Light},
                                                                Dark={DynamicResource Foundation5Dark}}"
                                    HeightRequest="48"
                                    StrokeShape="RoundRectangle 24"
                                    StrokeThickness="0"
                                    VerticalOptions="Start"
                                    WidthRequest="48">
                                    <Image Margin="8"
                                            Source="{Binding Icon}"/>
                                </Border>
                                <VerticalStackLayout Grid.Column="1"
                                        Spacing="5">
                                    <Label
                                        Style="{DynamicResource BodySemiBold18}"
                                        Text="{Binding Title}"
                                        TextColor="{AppThemeBinding Light={DynamicResource Text1Light},
                                                                    Dark={DynamicResource Text1Dark}}"
                                        VerticalTextAlignment="Center"/>
                                    <Label
                                        Style="{DynamicResource BodyRegular12}"
                                        Text="{Binding Description}"
                                        TextColor="{AppThemeBinding Light={DynamicResource Text2Light},
                                                                    Dark={DynamicResource Text2Dark}}"
                                        VerticalTextAlignment="Center"/>
                                </VerticalStackLayout>
                                <Button
                                    Grid.ColumnSpan="3"
                                    BackgroundColor="Transparent"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectPaymentMethodCommand}"
                                    CommandParameter="{Binding .}"/>
                            </Grid>
                        </Border>

                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>

        </Grid>
    </Grid>
</ContentPage>